#compdef ag

# zsh completions for 'ag'
local arguments

arguments=(
    '--ackmate[Print results in AckMate-parseable format]'
    {-A,--after}'[Print lines before match (Default: 2)]'
    {-B,--before}'[Print lines after match (Default: 2)]'
    '--nobreak[Dont print newlines between matches in different files]'
    '--nocolor[Dont print color codes in results]'
    '--color-line-number[Color codes for line numbers (Default: 1;33)]'
    '--color-match[Color codes for result match numbers (Default: 30;43)]'
    '--color-path[Color codes for path names (Default: 1;32)]'
    '--column[Print column numbers in results]'
    '--noheading[Dont display filenames as headings]'
    '--line-numbers[Print line numbers even for streams]'
    {-C,--context}'[Print lines before and after matches (Default: 2)]'
    '--nogroup[Same as --nobreak --noheading]'
    '-g[Print filenames matching PATTERN]'
    {-l,--files-with-matches}'[(dont print the matching lines)]'
    {-L,--files-without-matches}'[Only print filenames that dont contain matches]'
    '--no-numbers[Dont print line numbers]'
    '--print-long-lines[Print matches on very long lines (Default: >2k characters)]'
    '--silent[Suppress all log messages, including errors]'
    '--stats[Print stats (files scanned, time taken, etc.)]'
    {-a,--all-types}'[Search all files (doesnt include hidden files]'
    {-D,--debug}'[Ridiculous debugging (probably not useful)]'
    '--depth[Search up to NUM directories deep (Default: 25)]'
    {-f,--follow}'[Follow symlinks]'
    {-G,--file-search-regex}'[PATTERN Limit search to filenames matching PATTERN]'
    '--hidden[Search hidden files (obeys .*ignore files)]'
    '--ignore[Ignore files/directories matching PATTERN]'
    {-i,--ignore-case}'[Match case insensitively]'
    '--ignore-dir[Alias for --ignore for compatibility with ack.]'
    {-m,--max-count}'[Skip the rest of a file after NUM matches (Default: 10,000)]'
    {-p,--path-to-agignore}'[Use .agignore file at STRING]'
    {-Q,--literal}'[Dont parse PATTERN as a regular expression]'
    {-s,--case-sensitive}'[Match case sensitively (Enabled by default)]'
    {-S,--smart-case}'[Match case insensitively unless PATTERN contains]'
    '--search-binary[Search binary files for matches]'
    {-t,--all-text}'[Search all text files (doesnt include hidden files)]'
    {-u,--unrestricted}'[Search all files (ignore .agignore, .gitignore, etc.;]'
    {-U,--skip-vcs-ignores}'[Ignore VCS ignore files]'
    {-v,--invert-match}'[Show all lines that dont match pattern]'
    {-w,--word-regexp}'[Only match whole words]'
    {-z,--search-zip}'[Search contents of compressed files]'
    '*:filename:_files'
)

_arguments -s $arguments
